project('AnA', ['c', 'cpp'])

vulkan_dep = dependency('vulkan', required: true)  # Vulkan SDK
glfw_dep = dependency('glfw3', required: true)      # GLFW

subdir('Src/Core/')
subdir('Src/GUI')
subdir('Src/Editor/')