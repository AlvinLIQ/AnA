#version 450

layout(push_constant) uniform Push {
    uint sType;
    uint index;
    vec3 color;
} push;

struct Item
{
    vec3 position;
    vec3 color;
};

layout(std140, binding = 1) readonly buffer ItemIn
{
    Item itemsIn[];
};

layout(std140, binding = 2) buffer ItemOut
{
    Item itemsOut[];
};

void main()
{
    //vec3 test = itemsIn[0].position * vec3(2.f);
}